import React from "react";
import styled from "styled-components";
import Loader from "../../Loader";

const Container = styled.div`
  padding: 0px 40px;
  padding-top: 70px;
`;

const ListPresenter = () => (
  <>
    <Container>도쿄~</Container>
  </>
);

export default ListPresenter;
