// 처리된 데이터를 받아서 보여주는곳
import React from "react";
import styled from "styled-components";

const DetailPresenter = ({ result, error, loading }) => {
  return <h1>Detaillll</h1>;
};

export default DetailPresenter;
